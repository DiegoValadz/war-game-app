import { CardSymbolPipe } from './card-symbol.pipe';

describe('CardSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new CardSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
